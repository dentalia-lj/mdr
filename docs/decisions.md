# Decision log — every ruling this project depends on

Split out of `PHASES.md` on 2026-09-04, verbatim. A ruling that lives only in a
chat log is drift waiting to happen, so it lives here instead.

**Check this file before proposing anything that looks already-decided.**
`PHASES.md` § Decisions carries the most recent rulings and points here for the
rest. The narrative behind each row is in [build-log.md](build-log.md).

`By`: **D** = Denis · **C** = client · **A** = agent proposal, still `[FILLED — needs review]`.

| Date | Subject | Ruling | Consequence applied | By |
|---|---|---|---|---|
| 2026-08-31 | `[document-manufacturer-binding]` scope | **Every document gets a binding**, not only manufacturer-scope ones. The filed set becoming queryable is the larger prize: 540 documents reach no manufacturer through item links, 443 of them `filed` — every filed document, by definition. | Migration 053 writes at three GATE points, including the ordinary group-scope path and both C16 dispositions. | D |
| 2026-08-31 | `[document-manufacturer-binding]` blank-class item links | **No links written.** Binding only. `[mfr-bind-empty-class]` stands untouched: a blank BC device class is UNKNOWN, so no MDR coverage is asserted over items nobody has classified. | `_derive_mfr_scope_links` still requires `md_flag IS TRUE`; only the OFFER opened. Re-derivation when BC populates is filed as `[bound-document-link-rederivation]`, deliberately not in this design. | D |
| 2026-08-31 | `[document-manufacturer-binding]` read authority | **Column wins, link derivation fills gaps** where it is null. | `manufacturer_documents` reads the column, falling back to the item join; `include_unlinked` defaults OFF so a filed document never enters an existing consumer's payload unasked. | D |
| 2026-08-31 | Manufacturer picker restriction | **"Reviewer should be able to select what they want to select."** The picker offered 9 of 366 entities; withholding the rest was defensible only while a zero-link binding recorded nothing, which 053 fixes. | `_mfr_binding_options` drops its `md_flag IS TRUE` filter and counts MD items conditionally; `_mfr_unbindable_entities` and the "leaves N out" prose deleted; the blank-class review sentence stops telling the reviewer to wait for BC. | D |
| 2026-08-26 | `[playbooks-into-db]` storage shape | **Shred identity, jsonb the behaviour.** `bc_codes` / canonical name / `aliases` become constrained tables; the other 13 fields stay a `body jsonb`. Rationale: those three are exactly what `playbooks.validate()` polices, so shredding turns "authoring error found later by RESOLVE" into "save refused at 422" — which matters because non-devs will author these in the UI | Migration 049 (`manufacturer_bc_code`, `manufacturer_name`); `app/manufacturer_seed.py`. Spec `docs/superpowers/specs/2026-08-26-playbooks-into-the-database-design.md` §2.1 | D |
| 2026-08-26 | `[playbooks-into-db]` `extract_hints` in the UI | **Tier B — editable with hard limits**, not engineer-only. Length cap, keys constrained to `tiers.TARGET` + `"general"`, `_HINT_OVERRIDE` retained. It is the only playbook value an LLM sees (a second system block, `llm.py:196`), but zero of the 33 playbooks use it, so the risk is prospective and the migration cost is nil | Slice 3b. The key allowlist also lands in `_parse()` as a correctness fix — a typo'd key today silently emits a hint for a field the model was never asked about | D |
| 2026-08-26 | `[playbooks-into-db]` loader failure mode | **Never-raises splits by failure kind.** A malformed *row* is logged and skipped as today; a *source* failure (no connection) retries once then **raises**. Returning `()` on a dead DB would silently strip every manufacturer's rules at once — BACKFILL alone would archive Neodent's 28 Dentalia invoices, since `pb is None` stops both `skip_backfill` and `exclude` | Slice 2, `app/playbooks.py`. Spec §3.5 | D |
| 2026-08-26 | `[playbooks-into-db]` the 33 JSON files after the swap | **They stay in git, for reference** (Denis, 2026-08-26). Not deleted in slice 4 and not treated as truth after slice 2: the DB is authoritative, the files are the authoring record. 33 files' worth of rationale lives in their comments and nothing else carries it | Slice 4, `docs/superpowers/plans/2026-08-26-playbooks-into-the-database.md` Task 15 | D |
| 2026-08-26 | `[playbooks-into-db]` edit audit trail | **Required, and the mechanism for revert** (Denis, 2026-08-26). `updated_by` / `updated_at` on `manufacturer` (migration 049) plus `manufacturer_playbook_revision` (slice 2). Revert is not a delete — it writes the old body forward as a NEW revision, which is why the `dentalia_api` grant is `SELECT, INSERT` with no `UPDATE` and no `DELETE`: append-only is grant-enforced, not conventional | Migration 049 (shipped); `manufacturer_playbook_revision` slice 2 Task 5; revert slice 3a Task 10. Spec §2.1 | D |
| 2026-08-27 | `[playbooks-into-db]` `robots_refused.txt` after the mount goes | **A `refused_host` table** (Denis, 2026-08-27). Slice 4 moves it with everything else rather than shipping the file into both images by an explicit COPY. The point is the editor: a UI save can then be refused for the same reason a CLI one is, so a non-dev cannot author a `kind:"direct"` source on a host we have ruled we may never fetch (the 2026-08-20 ruling: 5 hosts, 4 manufacturers, 822 items) and only discover it when FETCH declines | Slice 4, plan Task 15. Spec §8 | D |
| 2026-08-27 | `[playbooks-into-db]` `note` on a UI save | **Required, refuse at 422 without one** (Denis, 2026-08-27). Not per-tier. The audit trail is only worth having if its entries say why: a revision list of timestamps and usernames does not make a revert obvious months later, which is the job it was ruled in to do | Slice 3a, plan Task 10 | D |
| 2026-08-27 | `[playbooks-into-db]` the D2 collision matcher | **Containment, not the fuzzy ratio the spec prescribed.** Measured, not argued: `fuzz.ratio('Sirona Dental Systems GmbH', 'SIRONA')` is 12.5 and the Maillefer pair 9.3, against `reconcile.SUGGEST_MIN_SCORE` = 80 — `_suggest` scores **zero** hits over every delivered playbook, including the incident it was chosen for, because the master carries bare brand labels and playbooks carry legal names. Also corrected: the check did not "exist nowhere" — `reconcile`'s `alias-collision` finding tested string *equality* and was blind for the same reason, and it is a report on a command an operator can skip | Slice 3b, plan Task 12. `app/playbooks.py::brand_collision`, armed on all three write paths (`playbooks sync`, `manufacturers seed`, the UI save); `playbooks validate` stays connectionless. Spec §D2 | D |
| 2026-08-27 | `[playbooks-into-db]` the two collisions the guard found | **Claim the code in both cases** (Denis, 2026-08-27). `ustomed` ← 10111 `ULRICH STORZ GMBH & CO. KG` (6 items; same catalogue numbering as 10119, and the playbook's own note already read the alias as a former name on register entry HRA 451050); `kuraray-dental` ← 10071 `NORITAKE` (1 item, `141080 NORI-VEST-SET`; `Kuraray Noritake Dental Inc.` is the legal manufacturer the playbook's note names, so dropping the alias would have cost every Kuraray DoC its signature name). Keeps the alias *and* covers the items. Effect on the seed, measured with the file set held constant: 384 → 382 entities, 383 → 381 named, 44 → 46 codes under a playbook | Slice 3b, plan Task 12. **6 `item_group` rows need `regroup` after the next `playbooks sync`** — `tasks/followups.md` `[orphaned-groups-ustomed-kuraray]` | D |
| 2026-08-27 | `[playbooks-into-db]` onboarding after the files retire | **Add "Start a playbook" to the UI, AND keep the file import working** (Denis, 2026-08-27, choosing between four options). The gap, verified rather than assumed: `manufacturer.slug` had exactly one writer (`manufacturer_seed`) and none of the nine `web/registry.py` routes created a playbook — so `/manufacturers?no_playbook=1` listed the ~350 that need one and clicking through offered nothing to author, and slice 4 was about to drop the directory holding the only remaining way in. **The spec's "the onboarding worklist already ships it — nothing to build" was wrong.** So: a `POST /manufacturers/{name}/playbook` that attaches a slug and an empty body at revision 0 (every Tier A/B control then applies, rather than a second authoring surface), and the `Dockerfile` COPY stays so `manufacturers seed` still runs in a container. Only the `web` bind mount goes | Slice 4, plan Task 14b + revised Task 15 | D |
| 2026-08-27 | `[playbooks-into-db]` how far the UI rename goes | **`[FILLED — needs review]`** The plan said the rename "renames and re-resolves in one transaction". It cannot: the re-resolve DELETEs `item_group` / `item_group_member` / `discovery_log` / `grouping_suggestion`, `dentalia_api` is SELECT-only on all four (invariant 1 — the UI is a job producer), and there is **no job type for a regroup**, so enqueueing it would be a PRD change rather than a decision to take here. **Proposal, implemented:** the action splits along the grant line — the UI writes `manufacturer.canonical_name` + `manufacturer_name` (demoting the old name to an alias, never deleting it), prices the rest with `regroup.rename_impact()`, and hands over the exact `dentalia regroup` / `dentalia playbooks sync` lines rather than pretending they ran. If a one-click rename is wanted instead, that is a new job type and a PRD change | Slice 3b, plan Task 13. Spec §D1 | D |
| 2026-08-27 | `[playbooks-into-db]` how the compose check runs | **Throwaway database and containers, not the shared dev stack** (Denis, 2026-08-27). Four worktrees share that database and those containers; 049/050 are additive but the eudamed session writes `manufacturer` and would start firing the epoch trigger, and the restart is not this session's to do | Slice 2, plan Task 9 last item | D |
| 2026-07-13 | G4 `mfr_ref` source | 44,5% blank, ~8% genuinely distinct | AC1 ceiling ≈8% strict / ≈25% per-supplier; `Ingest.mfr_ref_source_by_code` | A |
| 2026-07-15 | G6 HTML doc-library | Link **enumeration**, not content extraction — BS4, not trafilatura | Tiers D0/D/R in FETCH, LLM tier A in DISCOVER; MVP routes inconclusive → `manual_task` | A |
| 2026-07-15 | `vendor/distributor` source | Not a separate mechanism — it *is* `search` | No new job type, no handler case; promote to a pinned-domain variant only when a distributor-only manufacturer appears | A |
| 2026-07-23 | G3 UI/API auth v0 | HTTP Basic at a reverse proxy | `caddy` is the sole ingress; `X-Forwarded-User` → `decided_by` | **D** |
| 2026-07-23 | G16 UI reconciliation | Keep the shipped routes, drop `/enqueue` | `/ingest`, `/staging`, `/manual`, `/dead` stay as built; the CLI covers hand-enqueueing | **D** |
| 2026-07-23 | Weekly report (GAP A) | New closed-enum tag `report.weekly` | migration 013, `dedupe_key: report:{period_key}` | **D** |
| 2026-07-24 | md-unknown rows | **Middle path** — process only for CONFIRMED-manufacturer codes | ~20 codes, ~25% of catalogue; per-code ingest change **still unbuilt** (P2) | **D** |
| 2026-07-24 | K1 denominator | Keep **both** (strict + processed-scope) | `docs/specs/kpi.md` reports two coverage figures, not one | **D** |
| 2026-07-24 | Threshold calibration | Tune from hand-labeled samples **before** the run, not on it | Supersedes recalibrate-on-the-sweep in G5a/S1.3 (P4) | **D** |
| 2026-07-24 | AC1 in S1.7 | **Measure-only** — report against both ceilings | Pass/fail target set after BC/IT confirms G4 | **D** |
| 2026-07-24 | mfr-scope gate predicate | Stays `md_flag IS TRUE` | Processed unknowns get no mfr-scope auto-links until BC confirms class | **D** |
| 2026-07-31 | C6 supersession persistence | **At production transition** | migration 015; applied by whichever of `gate.candidate` or `gate.apply approve` first reaches production, re-checked against a live DB read | **D** |
| 2026-07-31 | Evidence vs `extract_rev` | **Evidence per rev** | migration 014; idempotency key `(doc_id, field, extract_rev)`; "current" = MAX(rev) | **D** |
| 2026-07-31 | Evidence `page` | **T1/T2 only** | T0 filename-derived and T3 human evidence are legitimately pageless (invariant 2) | **D** |
| 2026-07-31 | Storage default | Flipped `gdrive` → `local` | `LocalFsStore` is live; `GoogleDriveStore.put()` raises until G7 | **D** |
| 2026-08-13 | C12 — the matching comparand | **The primary item number is the key**, and it is what is printed on the physical article | `ref-item` basis (migration 023); invariant 3 amended | **C** |
| 2026-08-13 | Flag classes | Flags are **three** classes — blocking / capping / informational | An informational flag no longer gates. The blanket rule had held 43 documents and all 309 live links at `staged` | **D** |
| 2026-08-13 | CARL MARTIN | A real manufacturer, bought direct from Germany | A document request is the live route; DISCOVER is the fallback | **C** |
| 2026-08-13 | NEODENT corpus | The folder is a mess — skip it | `skip_backfill` playbook key; the folder is **refused**, not filtered | **C** |
| 2026-08-17 | C16 machine binding | Write `mfr-scope` links without a human when the name resolves to exactly one canonical | Now the dominant basis — 6.295 of 8.280 production links — behind three guards added after a milling-unit declaration bound 1.069 ceramic-block items. On 2026-08-21 it bound one Carl Martin ISO certificate to 2.567 devices in a single step | **D** |
| 2026-08-18 | G9 renewal horizon | **30 days for every document type alike** | `Renewal.horizon_days` `(90,60,30)` → `(30,)`; may want raising once the loop has actually run | **C** |
| 2026-08-18 | When a document lapses | `LEAST` of stated expiry, cited certificate's expiry, issue + 5 years — not a fallback chain | migration 027 `document_effective_expiry`; reconciled three drifted implementations | **C** |
| 2026-08-19 | G17/G11 Zagreb | **No separate Zagreb.** One BC, one numbering, one pipeline | Phase 2 retitled, S2.5 re-scoped, G11/G17 closed, unowned-item 7 struck. No ZG adapter, no mapping layer, no cross-catalogue dedupe | **D** |
| 2026-08-19 | Messy corpora generally | **Skip beats filter** when a dump is disowned | `exclude` shipped but now has **no author** — delete it if the next messy folder is also skipped | **D** |
| 2026-08-19 | VOCO | Run it; it closes the manufacturer | 30 of 31 items, $12,50, done | **D** |
| 2026-08-19 | DENSTPLY | Do not run before `MAX_VISION_PAGES` — and, 08-20, do not run at all | ~1 item for $18–24 with half the corpus truncated and nine new type defects | **D** |
| 2026-08-20 | S1.7's method | **The corpus sweep was never the goal it became** | One manufacturer at a time behind a go/no-go pre-flight; two refusals are the gate's output, not exceptions to it | **D** |
| 2026-08-20 | Registry data (EUDAMED, GUDID) | **Secondary source of truth, never primary** — 99,0% agreement over 240 devices makes a good corroborator and a poor authority | `ref-eudamed` belongs INSIDE `item_document_trusted_basis_ck`, capped at `staged` — **the ruling still owed** (§Now § Closed — `eudamed.sync`; ruled 2026-08-25, below) | **D** |
| 2026-08-20 | Registry fetching | **No sweep ever runs unattended.** Serial where no rate limit is published; prefer a bulk download to an API; an operator releases every run | Contradicts PRD §3 / handbook §9, which put `eudamed.sync` on its own cron — recorded as a binding constraint | **D** |
| 2026-08-20 | Basic UDI-DI trust | A code that fails its own check pair is not trusted at 1.0 | `app/extract/udi.py`; confidence scored, `basic_udi_check_failed` anomaly, offline and free | **D** |
| 2026-08-20 | Outbound email | **The system never sends** — drafts only, a person sends from `mdr@dentalia.si` | No send handler; G8 narrowed to read access only | **C** |
| 2026-08-20 | Renewal cadence | At most one mail per manufacturer per week/fortnight, listing every document expiring for them | Changed the schema: `renewal_request_document`, partial unique index on `(manufacturer, period_key)` | **C** |
| 2026-08-20 | Contacts vs crawl URLs | **Contacts in the database, crawl URLs in the playbooks** | Boundary rule: a playbook never contains an email address, and a crawl URL is **playbook content, never contact data**. `manufacturer.playbook_ref` is the join. **Amended 2026-08-27 (Denis): the second half was written as "the database never contains a crawl URL", which `050_playbook_body.sql` made false** -- it stores `doc_sources` in `playbook_body`, so crawl URLs now live in a jsonb column. The ruling was about which artefact OWNS a URL, not which store holds the bytes; the boundary is authoring home, not storage. **Amended again 2026-09-03 (Denis): a playbook MAY carry ROLE mailboxes** — `Playbook.contacts`, seeded into `manufacturer.contact_emails` by the seed importer, which stays the single READ path (`app/playbooks.py:231-237`). Personal addresses are refused at authoring time by `app/contacts.validate_for_playbook`, at both doors (file load and UI save): a playbook is a durable artifact in git, a named employee goes stale, and their address is personal data in a repository. So the absolute above is retired — what never belongs in a playbook is a NAMED INDIVIDUAL, not an address | **D** |
| 2026-08-20 | Invariant 12 | Widened to cover cheap-tier summarisation of inbound email bodies | The summary is UI context — never evidence, never a production value, never a GATE or VALIDATE input. **Ratified same day.** *(Corrected 2026-09-16: the `[NEEDS REVIEW]` marker this row claimed is in neither CLAUDE.md nor the PRD — invariant 12 reads "ratified by Denis the same day" and PRD §4 says "widened and ratified 2026-08-20".)* | **D** |
| 2026-08-24 | robots `Crawl-delay` vs the politeness lease | **A new `min_politeness_ms` column on `domain_lease`**, written only by the robots reader; the lease takes `GREATEST(caller, min_politeness_ms)` | `try_domain_lease` rewrites `politeness_ms` from the caller on every acquire (`app/queue.py:247-265`), so a crawl-written 10s delay for `renfert` is stomped back to the global 2s by the next `fetch.url`. Config is the floor, robots raises it, the robots TTL refresh lowers it. A change to the shared FETCH path; prerequisite for §4.1 layer 3 of the crawl-recipe design | **D** |
| 2026-08-25 | `ref-eudamed` capping, and EUDAMED scope | **Capped at `staged`** — `ref-eudamed` joins `item_document_trusted_basis_ck`'s blacklist, so an EUDAMED catalogue-number match can never reach production without a human. EUDAMED is a secondary source, never primary. Scope ruled **stage 1 only**: fetch, store, display; no link writing | Closes the one ruling S2.3 was blocked on (§Now § Closed — `eudamed.sync`). `app/handlers/eudamed.py` + migration 038 (`eudamed_mirror.reference`) + a per-document button land stage 1; `eudamed.sync` leaves the noop list. No link is written yet, so the constraint amendment ships with stage 2, not now. Note the rung is still dark: DISCOVER reads `cert_refs` and EUDAMED publishes no document URLs (measured 2026-08-20), so filling the mirror does not light it | **D** |
| 2026-08-24 | Completeness card requirement matrix | **Score the Art. 14(2) duties, not the certificate.** Dentalia is a *distributor*: DoC required for every device; IFU required, `CONDITIONAL` (amber) only for class I and IIa under the Annex I 23.1(d) exemption and never for Ir, whose reprocessing IFU sits inside the notified body's remit (Art. 52(7)(c)); notified-body **number** required wherever a NB is involved, satisfied by the number printed on the declaration (ECJ **C-10/24**, 4 June 2026 — plausibility check, not correctness). EC certificate and ISO 13485 shown, never scored. UDI deliberately not scored | New module `app/compliance.py` (**not** `config.py` — a legal duty must not be env-var-overridable); `web/registry.py` `item_completeness`/`manufacturer_completeness`; cards on `/items/{ref}` and `/manufacturers/{name}`. `EXPIRY_HORIZON_DAYS` = 30, drift-guarded against `Renewal.horizon_days`. A lapsed `basis='staleness'` date renders **`review-due` (amber), never `expired`** — it is Dentalia's five-year house rule, not an MDR expiry. Client-facing writeup: `docs/2026-08-24-what-the-system-does-{en,sl}.md` | **D** |
| 2026-08-24 | `stated_class` boilerplate false positive | **Ruled "let it stand" — then a guard was built the same day. Unreconciled.** | The measured case is doc **389**, a TÜV SÜD MDD EC certificate, `staged` and expired 2024-03-29, reading `class III` off a sentence about class III devices in general. Denis ruled no suppression heuristic: the candidate guard would also kill genuine self-statements ("this is a Class IIa medical device"), the field is QA-only, and the cost was one dismissible `conflict` row. **A change the same day then shipped exactly such a guard** (`_CLASS_DEVICES_TAIL` + a `classified as` rescue), citing "the Denis ruling" — a different instruction, given in another session. Live effect, verified: 389 and 791 now abstain, 248 and 439 gained `IIa`. **Someone has to say which ruling stands** before that guard is extended; residue in `[stated-class-replay-left-two-disowned-values]` | **D / A** |
| 2026-08-24 | C17 link-level human decisions | **A capped link needs a human verb, and had none.** `manual` was enumerated as a production-capable `match_basis` from PRD v3 and **nothing ever wrote it** — so a `fetch-context` / `name-family` / `ref-catalogue` link could be neither published nor refused, and approving its DOCUMENT left it staged forever. Found by Denis on item **2222100** / doc **843**, where the review read as ignored | `gate.apply` gains `confirm-link` / `reject-link` / `reopen-link`, keyed on `item_ref`, document untouched; `confirm-link` is the sole writer of `match_basis='manual'`. Companion fix: `rejected` joins `production` in `STICKY_LINK_STATUSES`, closing a resurrection hole that predates C17 (the v3 reject cascade's links were silently un-rejected by the next candidate); `retracted` stays non-sticky on purpose. `reopen-link` is the human-only way out of `rejected`. Buttons on `/staging` and the item page via `web/templates/_link_decide.html`. Measured queue: **1** link actually affected of 8.563 — a correctness hole, not a workload | **D** |
| 2026-08-24 | Staged links under terminal documents | **Reported as a defect, WITHDRAWN the same day — no status change.** 17 `map-supplier` links sit staged under docs 375/379, which the task-5 fast path auto-filed into the `375/379 → 415 → 377` chain. Each was `filed` first (C15 requires an empty `links` payload) and reached `superseded` on a later coverage-map rev; **neither was ever `production`**. Their links are staged because their document is not published — the consistent state | Promoting them would assert production coverage under paperwork the registry never published. Ruled read-only visibility instead: `_staged_links_on_unpublished` + a formless table on `/staging`, because invisible dead state reads as state nobody considered | **D** |
| 2026-08-26 | EUDAMED stage 2 acquisition | **The register is mirrored whole, not queried per manufacturer.** `eudamed.certcheck {manufacturer}` becomes `eudamed.certregister {}`: 16 calls at `size=300` pull all 4.608 certificates, and `actorSrn` arrives on every record, so **SRN discovery is a by-product of the certificate pull, not a prerequisite for it** — the first spec draft ordered these backwards. Matching happens locally via pg_trgm → rapidfuzz, never through a substring query | Live re-measurement 2026-08-26 corrected the draft: the register is **4.608 certificates, not 400**, and carries **358 IVDR** certificates the first count missed entirely. `actorName` *does* filter and is a **substring** match (`Ivo`→4, `Ivoclar`→1); there is **no actor-search endpoint** (six candidate paths all 302); max page size is **300, not 100**, so Ivoclar's device sweep is ~38 pages, not 114. The draft's central conclusion survives untouched: no DoC exists in EUDAMED in any form. Article probing survives only as the fallback for device manufacturers holding no certificate — GC EUROPE, measured zero | **D** |
| 2026-08-26 | Unattended sweeps | **The 2026-08-20 ruling is restored, with one carve-out.** The scheduler *proposes* — it computes `due_at` and marks a manufacturer due — and a human releases every device sweep. `eudamed.certregister` is carved out and runs on the tick: 16 calls against a public register is the *"prefer a bulk download to an API"* the same ruling asks for, not the per-manufacturer sweep it forbids. Cadence: certificates **monthly**, device sweeps **quarterly** | The first spec draft proposed a self-emitting monthly tick behind a default-off flag, which contradicted a decision-log entry marked binding. Caught in brainstorm, not in review. Adds `last_swept_at` / `due_at` / `released_at` / `released_by` per manufacturer, and the due list is itself a view worth having | **D** |
| 2026-08-26 | Certificate-number normalisation | **Join on baseline, report near-misses.** The drift view joins only on the safe split (`Rev. NN` separated; the ` R000` R-code already stripped by `validate.py`'s `_RCODE_SUFFIX`). Looser candidates — a trailing single letter, an `SX`→`HZ` prefix — surface as a `possible_match` column for a human to confirm, never as a join | Measured: 7 of our 58 distinct `cert_number` values match the register, covering **107 of 391** certificate-carrying documents. The trailing-letter rule would take it to 143, the prefix rule to 145; stripping ` R000` gains **nothing**, since `MDR 778483` is absent from the register outright (that pair is an *internal* join `_RCODE_SUFFIX` already solves). The 2026-08-19 ruling stands untouched. Reach is capped by our extraction quality, not by EUDAMED: `0482`, `CE-0197`, `NB1639`, `Quality Management System` are sitting in `cert_number` and are not certificate numbers | **D** |
| 2026-08-26 | EUDAMED findings, and actor attribution | **Four findings, not two**: revision drift, **`certificate_status_alert`** (a nine-valued `certificateStatus` nobody had looked at — 87 `withdrawn`, 68 `cancelled`, 31 `restricted`, 18 `suspended`), **`certificate_gap`** (EUDAMED lists a certificate we hold no copy of — Carl Martin's `HZ 1594091-1`), and the declaration gap. All views, none stored. Attribution of an actor to a canonical manufacturer is **exact-auto, fuzzy to a confirm queue** | `manufacturer` gets its first writer, seeded for **all 384** canonical names, plus `srn_probed_at` so "probed, none exists" stops looking like "never probed". `eudamed_mirror` gains `first_seen` (never touched by `ON CONFLICT`) and `device_status_type` — today `synced_at` is stomped on every upsert, so **no delta is derivable at all**. Two live findings the draft did not have: Straumann `G10 020326 0062` ours Rev. 06 vs **Rev. 07** `reissued`; Brasseler `HZ 1470094-1` at **Rev. 5** with an expiry of **2026-02-28**, six months past. Spec: `docs/superpowers/specs/2026-08-25-eudamed-enhancement-design.md` | **D** |
| 2026-09-03 | Device class, who fills it | **We find it ourselves** — either BC supplies it, or we extract it from the documents we parse. Not a blocking ask on the client | Round-3 question 5 closed. The "prepare a per-article list for them to import into BC" proposal is no longer a dependency; class arrives with documents, manufacturer by manufacturer | **D** |
| 2026-09-03 | KOMET price list | **Arrived and used.** | Round-3 question 8 closed. The conversion was already built and matching 227 of 319 articles without it; the list served as the cross-check it was wanted for | **D** |
| 2026-09-03 | Round-3 client questions | **All old, answered, and covered in bulk.** `docs/2026-08-13-vprasanja-za-dentalio.md` is a historical record, not an open ask | The "6 open client questions" figure is retired. Do not re-count it as blocking work; G7/G8 remain open as gaps in their own right | **D** |
| 2026-09-03 | `DISCOVER_HOLD` | **Stays true.** Run only a fraction of the catalogue at a time, never the whole database | No global unhold. Scoped runs continue to work with the hold on — it gates only the RESOLVE→DISCOVER emission, and a directly enqueued `discover.group` bypasses it | **D** |
| 2026-09-03 | Dead jobs | **Re-run them and test whether they still fail.** | Done the same day: 54 rows = 18 units, **17 recovered, 1 genuine DNS failure**. $0,5071 spent. See the State block | **D** |
| 2026-09-03 | What the BC data gaps mean for coverage | **Recorded as context, not raised as a claim.** The numbers explain a result; they are not an argument about anything else | BC carries neither MD flag nor class on 11.693 of 15.958 items (73,3%), and 3.019 of 4.265 MD items have no `mfr_ref` (70,8%). That is why article-level matching covers 36% of the catalogue and not most of it. The measurements stay recorded so the coverage figure is explicable; nothing further is derived from them | **D** |
| 2026-09-03 | Registry + archive backup | **Not built, and deliberately not scheduled** | The archive exists and is under Dentalia's control; nothing backs it up. `[no-registry-or-archive-backup]` stays open in the tracker as a **standing operational risk, never as scheduled work**, so it is never mistaken for something in progress. Round-3 client question 19 already asks their IT whether the server is backed up, which may cover part of it | **D** |
| 2026-09-04 | Same-day revisions (audit §09 C) | **Flag and force manual.** Two production candidates for one subject, type and regulation with an identical `validity_from` have no order the dates can express; neither supersedes by machine | VALIDATE emits `same-date-revision` (blocking), GATE routes to `manual`, the reviewer keeps the later revision. Three groups already held triplicate production DoCs on one date when this was ruled; until then the equal case matched no rule | D |
| 2026-09-04 | Discovery review load (audit §10) | **Keep coverage-scan at 25 groups/day; a reviewer has to be named before the queue can drain.** The cron stays on and the review queue grows until someone is | Recorded here and in the claims register; no code change. `.env` already carries `SCHEDULER_COVERAGE_SCAN_ENABLED=true` | D |
| 2026-09-04 | Crawl link triage and document-type preference | **Deterministic triage first (`link_pattern`, `doc_type_from`, `max_links`); a collection is judged link by link — type and article numbers — and the budget is spent on links naming our own articles first, then DoC → EC → ISO → IFU. IFUs are kept, never filtered** — same ruling as `[discover-should-rank-doc-ec-iso-above-ifu]`. Item relation from anchors is a weak signal (a DoC's anchor names none of its 900 REFs), so absence is neutral; the REF gate at VALIDATE remains where item relation is decided | Built the same day: the judge and its order, plus migration 061 (judged once per library version). PRD §3, handbook, `playbook-authoring.md` say so | D |
| 2026-09-11 | Office UI word list | **Adopt the proposal's word list** (Published, Waiting for review, On file, Replaced, Failed, Missing documents, Renewal emails, Emails received…). Stored values do not change, only the screen word | `web/words.py` holds the one mapping; spec `docs/superpowers/specs/2026-09-11-office-ui-redesign-design.md` § 9 | D |
| 2026-09-11 | Office UI logins | **One Caddy login per person, plus a short operator list** (`web.operator_users`). While the list is empty every login is an operator, so the shared login keeps working | Spec § 2 and § 7 (P5b); who the office users are is Denis's input | D |
| 2026-09-11 | Operator screens | **Hidden from office logins. Four writes are refused for non-operators:** playbook edits and code claims, BC push apply, scheduler run/arm, `/ingest`. The pages themselves stay reachable by URL; office keeps `/import` | Spec § 2 D3, § 7 | D |
| 2026-09-11 | Coverage headline | **"Declarations on file for X of Y medical-device items (Z%)"** replaces the 99.7% any-document figure; `Y - X` equals the Coverage gaps "no declaration" count | Spec § 8 | D |
| 2026-09-11 | Review decisions | **Decide only with the document open.** No Approve or Reject on a collapsed row; a whole-range approval confirms with the count; a reject carries a reason as the optional `gate.apply` `note` | Spec § 3 (P1a); additive payload field | D |
| 2026-09-11 | Who may re-run failures | **Office: only "took too long". Operators: everything.** Dead addresses get "search again" (discovery per group), not a re-run | Spec § 4 | D |
| 2026-09-11 | Expiring window | **30 days, labelled wherever an expiring figure appears**; the strip, the Expiry page and the weekly report share one constant | Spec § 8 | D |
| 2026-09-11 | AC1 target (G15) | **50% of BC-flagged medical-device items (`md_flag IS TRUE`) hold a production DoC linked at article level** (`ref-list`, `ref-item`, `basic-udi-di`, `map-supplier`). Baseline the same day: 1,460 of 4,265 (34%); 488 of those DoCs unexpired | Target recorded; not yet a KPI on the board | D |
| 2026-09-11 | AC3 target (G15) | **At most 5% wrong (10 of 200)** in an audit of 200 randomly sampled auto-written links, each checked against its PDF by Nataša. The same labelled sample feeds the calibration map (`[gate-calibration-map-absent]`) | Target recorded; the audit has not run and no table holds its results | D |
| 2026-09-11 | Class I at issue + 5 years | **Review due, not expired.** A self-certified class I DoC with no stated expiry and no certificate turns amber "review due" at issue + 5 years and gets a "please confirm this DoC is still current" draft, not a renewal demand; the item is not shown as uncovered. Settles the Q14/Q16 conflict; Q16 is not re-sent | Not built: `document_effective_expiry` (migration 027) still expires every such DoC as `basis='staleness'`; `[class-i-review-due]` | D |
| 2026-09-11 | Completeness card matrix | **Accepted as final.** The 2026-08-24 Art. 14(2) matrix is the rule; it is not put to Dentalia | `app/compliance.py` unchanged; closes `[completeness-card]` (matrix) | D |
| 2026-09-14 | AC1 target, lowered (G15) | **33% of BC-flagged medical-device items (`md_flag IS TRUE`) hold a production DoC linked at article level** (`ref-list`, `ref-item`, `basic-udi-di`, `map-supplier`) — the same definition as the 2026-09-11 row, the target lowered from 50%. Measured the day of the ruling: **1.460 of 4.265 = 34,2%**, so it passes on the reading that set it | Supersedes the 50% of 2026-09-11. Nothing was promised to the client at 50%: offer §6 says N is set from phase-0 data and confirmed before phase 1, and that confirmation never happened. Two other units were put to Denis and not taken — device GROUPS with an article-level DoC (415 of 1.396 = 29,7%), which is the offer's own wording, and unexpired only (488 = 11,4%). Open: AC1 is on the status board; the AC3 audit still has no table | **D** |
| 2026-09-14 | AC3, audited error rate (G15) | **Met by construction, not by sampling.** The error rate on auto-written links was driven down by fixing its causes one at a time — the REF gate, C12 `ref-item`, C16 machine manufacturer binding, C17 link-level decisions, the capped bases (`name-family`, `fetch-context`, `ref-catalogue`, `ref-eudamed`), the non-device REF branch, T0 document typing, NEODENT's template, the self-comparison at revalidation — each with a test. Denis: **the gradual improvements did it**; no 200-link audit is required to close AC3 | The 200-link sample and the table to record it in are **not** to be built. Consequence for `[gate-calibration-map-absent]`: that sample was the only planned source of calibration factors, so calibration needs a different source or the multiplier goes. The audited evidence that does exist is small and pre-dates most of those fixes: 11 documents ruled on by a human, 8 approved and 2 rejected, both rejects on 2026-08-25 | **D** |
| 2026-09-14 | AC6, discovery reach (G15) | **Re-based on the scope we actually work, and met.** Reach over the whole 8.208-group catalogue is not a reasonable measure — most of those groups are not devices and were never meant to be searched. Denis: *"discovery reach cannot reasonably be counted, what we did on corpus is enough — adapt."* The criterion is now **the share of BC-flagged device groups holding a production document, by any route**, corpus and live discovery counting alike | Measured the day of the ruling: **1.391 of 1.396 = 99,6%**. The old reading (227 of 476 searched groups, or 227 of 8.208 catalogue groups) is retired and must not be re-quoted as AC6. The separate coverage question — how many of those documents are article-level rather than manufacturer-scope — is AC1's, and is answered there | **D** |
| 2026-09-14 | Supersession semantics (C6, invariant 5, AC8) | **Per item, not per document.** A newer declaration replaces an older one **only for the articles it names**, only when it is genuinely newer, and only within the same type and regulation. Denis: *"we can't request a manufacturer to always cover all the same devices — what if some go out of production? The documents can't always cover all the same items."* A supplier retiring an article is ordinary product lifecycle, so a rule that demanded the newer document name every article of the older one would treat the normal case as an anomaly and send it to a human. The document's own status turns `superseded` only when no production link of its own survives | **Settles AC8's third clause, which fails on live data: all four supersessions in the registry cross coverage subjects, three of them sharing no article at all.** Measured the same day over every pair of production documents sharing a group, type and regulation — the pairs today's code treats as candidates — **41 pairs: 11 identical article sets, 1 superset, 1 partial overlap, 28 with no shared article**. The group is the wrong subject 68% of the time. Chosen over the cheaper alternative (supersede only on an identical or superset article set, no migration, no contract change) precisely because of the ruling above. Work: `item_document` gains a link-level `superseded` status and `superseded_by`; C6 amended from "coverage subject" to the article set; every reader of `document.status='superseded'` learns the link-level state, or it shows a document as current for articles it no longer covers. Then one audited repair: 162, 375, 377, 379 and 415 back to production, 815, 832 and 839 rejected, and the 94 items get their declaration back. Spec first | **D** |
| 2026-09-14 | Route to the coverage target | **Email first, automatic retrieval second, manual last.** Ask the manufacturer for the groups and products we hold no declaration for; only what the letters do not bring is worth building a retrieval path for; whatever neither produces is entered by hand. Denis: *"let's focus on getting 60% by email — so the email template must include groups or products we need info for, then next is automatic retrieval, then if nothing else works, manual for the rest"* | The gap-request letter already composes that way (`email_request.compose_gap`: Basic UDI-DI groups, each with three of our own article numbers underneath). Sizing the same day: **2.805 flagged device items hold no article-level declaration, and CARL MARTIN is 2.567 of them (91,5%)**; everything else totals 238. Without that one supplier the ceiling is 1.698 of 4.265 = **39,8%**, so no target above 40% is reachable by any other route. Do not spend the budget on discovery breadth before the letters have been sent | **D** |
| 2026-09-14 | Who Dentalia writes to at a supplier | **The role mailbox, not the named regulatory officer.** Denis, asked to choose between Carl Martin's published PRRC (`v.gerlach@`), deputy PRRC and general box: *"probably info"*. `info@carlmartin.de` is authored in `playbooks/carl-martin.json` and written to `manufacturer.contact_emails`; draft 26 is addressed and releasable | Consistent with the 2026-09-03 amendment above, and the reason it exists: a named officer goes stale and is personal data. **Only 6 of 384 manufacturers carried any address before this**, so the same empty field stands between Dentalia and every other supplier letter the system will write | **D** |
| 2026-09-15 | Currency of a document, per item (C6, invariant 5, AC8) | **A document is current for the items it covers, and only for those.** Denis's three clarifications on the 2026-09-14 per-item ruling: (1) a document being current for some of its items and not others is **designed, not a defect** — *"the document is current while it covers some of the items"*; (2) **the asking entity is the ITEM or the MANUFACTURER, never the document** — *"document is an internal entity which needs even partial accuracy, but from item or mfr perspective we need clean relations and only the last document is current per each type"*; (3) an outdated document is **kept and not returned**, unless a caller asks for history — *"they are retrieved by api, but only if they need all"* | Consequences. The default API answer stays **one current document per type per item**; a new **"all" mode** returns the full list including retired ones, which is an API addition that does not exist today. Mechanism preferred on this reading: **latest-wins computed in the view**, not a persisted link-level `superseded` status — it needs no migration on `item_document`, no audited repair of the four bad supersessions (they stop being consulted), and removes the class of bug where a stored flag says the wrong thing. What it gives up is the queryable `A was retired by B` pointer, which the GATE audit log can carry as history without being the read path. Spec on that basis; persisted per-item supersession recorded as the rejected option | **D** |
| 2026-09-16 | Missing device class (F9) | **Expected, not a defect. The system is eventually deterministic.** Denis: *"the system is eventually deterministic which means we expect the issues like missing class for now. But as we grow, we will update and add the device classes. This is the goal."* An absent class is the state of a registry still converging, and the class arrives with the documents, manufacturer by manufacturer | Retires the 11.693-item figure as a finding; it stays a measurement (`data_anomaly` kind `md_class_blank`) and stops being scheduled work. Consistent with the 2026-09-03 ruling that we find the class ourselves rather than asking the client. Does NOT retire F34's class I review horizon, which is a property of a document and not of an item | **D** |
| 2026-09-16 | What a supplier reply does to a chase (F31) | **It pauses the chase and puts it in front of a person. It never closes one.** A reply is not an answer — *"we are looking into it"* is the commonest response to a renewal chase, and only a person can tell that from a real one | Deciding from the body would put an LLM summary into a state transition, which **invariant 12 forbids**: the summary is UI context, never a GATE or VALIDATE input. Work: a reply linked to a live `renewal_request` stops the reminder ladder and marks the request as waiting on a person; the four chases stopped at the escalation cap are re-armed once the rule exists, so they stop for a reason rather than by running out of rungs | **D** |
| 2026-09-16 | Confidence calibration (F33) | **Keep the mechanism, ship every factor at 1,0, and say on the page that confidence is the model's own number and uncalibrated** | The 200-link audit was the only planned source of labelled data and the 2026-09-14 AC3 ruling retired it. Dropping the multiplier would mean re-deciding where it belongs the day a labelled sample exists. **The wording is not optional**: a factor of 1,0 with no note reads as "calibrated, and it came out at 1" | **D** |
| 2026-09-16 | The five blockers (§ 2.1) | **None of them blocks delivery.** Re-tested against one question — does this stop us shipping — and ruled one by one. **B1 backup:** *"a thing for someday, not today"*, consistent with the 2026-09-03 out-of-scope ruling. **B2 alerting:** accepted as is — the in-queue half shipped, the out-of-body check stays missing because *"they will run another app on the same server so they will notice issues"*. **B4 acceptance:** *"done if you ask me, mark as sufficient"* at eight of nine. **B5 review debt:** *"their Nataša"* — Dentalia's reviewer, not our work | § 2.1 keeps all five listed because each is real and a reader needs to know who owns them; none is presented as work owed. B3 was withdrawn separately the same day | **D** |
| 2026-09-16 | B3, "INGEST has never run" | **Withdrawn — the claim was absence of evidence, and the thing it described is not a gap.** `item_mirror` holds 15.958 rows stamped 2026-08-10, so INGEST has run; the `job` table starts at id 16901 with 3.722 ids missing because early rows were deleted by hand. Denis: *"we used csv yes. and sftp corpus"* | The CSV route is the offer's v1 and works. `_tick_ingest_monthly` enqueues the newest file in `ingest_watch_dir` and is inert only because that directory is unset — deployment config, not a build gap. OData stays *kasneje* (item 4) | **D** |
| 2026-09-16 | The eight documents behind AC8 (F23) | **Repaired the same day, through `gate.apply` rather than by hand.** 815, 832 and 839 rejected — seeded test files from `tools/seed_fake_mailbox.py`, and under latest-wins 815 would have WON its item's pick. 162, 375, 379 and 415 restored to production; 377 left alone because it already was, and approving it would have re-run the supersession | Invariant 1 respected: seven `gate.apply` jobs, 14 audit rows, no hand-written registry update. **AC1 moved 1.460 → 1.571 of 4.265 (34,2% → 36,8%)** — the 94 items whose declaration was hidden behind a wrong supersession. The `superseded_by` pointers on the restored documents are left as the historical record of what the old rule did; F23's spec rules that column write-only history | **D** |
| 2026-09-18 | Ingress on the client server | **Two names through the host Caddy: `cw.dentalia.si` for the office, `api.cw.dentalia.si` for machines.** The API name serves only what `web/access.py` opens to a non-staff caller; `WEB_PUBLIC_BASE_URL` is the API name and is fixed before the first BC push, because it is written into the links BC stores | `Caddyfile` `@api_host_refused` + `tests/test_access.py`; the host-Caddy block and reload order in [2026-09-18-server-audit.md](2026-09-18-server-audit.md) § 6.1; [dev/deployment.md](dev/deployment.md) § 7 | **D** |
| 2026-09-18 | Migration 008 edited in place | **Allowed, as a stated exception to never-edit-an-applied-migration.** 008 now sets the `dentalia_api` literal only when the role has no password | Every fresh database migrated in the cluster re-ran it -- `schema-drift` on each deploy, the test suite, the rehearsal -- and put the published literal back over a real password (measured: the verifier changed across one `schema-drift` run). A new file cannot fix a file that runs on fresh databases; a role password is invisible to `schema-drift`, so no database diverges. `tests/test_migration_api_role.py`; [dev/changing-things.md](dev/changing-things.md) records the exception | **D** |
| 2026-09-18 | Deploy mechanics | **Every package pinned; a dump before every migration, restored into a scratch database before it counts; a prod overlay with `worker` capped at 3g, rotating logs and the archive on `ARCHIVE_HOST`.** The four orphaned `_eudamed_*` array types were dropped from the dev database the same day so its dumps restore strictly | `constraints.txt` + `tests/test_build_pins.py`; `scripts/deploy.sh` (host-side tree hash, dump, trial restore, `backups/deploy.log`); `docker-compose.prod.yml` + `tests/test_compose_config.py`; runbook § Deploy the working tree | **D** |
| 2026-09-24 | BC over plain HTTP from the server | **Accepted.** BC answers at `mail.dentalia.si:7048` over HTTP, not HTTPS, and the request of 2026-09-18 had asked for HTTPS. The port is closed to everything but `91.98.42.140` (measured: it answers from the server and not from elsewhere), and that allowlist is the protection relied on. Recorded as a known property: the Basic-auth credentials and the payloads cross the internet between the two sites unencrypted | `docs/dev/deployment.md` § 1 states it as measured and ruled; nothing in the tree requires TLS for the BC client | Denis |
