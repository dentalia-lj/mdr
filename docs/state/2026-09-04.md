# State reading — 2026-09-04

Read straight off the dev Postgres at the 2026-09-04 tree, 06:15–07:10 UTC, and
swept at 11:00 against a later commit the same day. Split out of `PHASES.md` on 2026-09-04: the plan
carries the headline numbers and links here for the reading behind them.

**A dated snapshot, not a live document.** Nothing here is maintained after the
date in its title; a newer reading gets a new file and `PHASES.md` repoints. Where
this disagrees with `PHASES.md`, `PHASES.md` wins.


**Swept 2026-09-04 11:00:** four items this block listed as open had already closed in commits that predate the sweep, one of them only in part. Corrected in place below; the database figures were not re-measured in that sweep.

Read straight off the dev Postgres, not from notes, and it is the **only** current-state block in this file — earlier dated snapshots are deleted rather than kept as trajectory. The previous block was read 2026-09-03 against the 2026-09-02 tree; **55 commits landed between the two readings**. Full audit with evidence per finding: readiness audit artifact, 2026-09-04. Claims the previous block made and this one overturns:

- ~~"0 of 384 manufacturers carry a contact"~~ — **six do**, and the outbound chain picks them up (proven live, below).
- ~~"`docker compose down -v` destroys the registry and the archive"~~ — **it destroys the archive only.** Postgres is a host bind mount and survives; the 676 MB named volume `dentalia_archive_data` does not. The result is a live registry whose every `archive_url` points at nothing, which is worse than losing both.
- ~~"G6 (HTML doc-library link extraction) is open"~~ — **functionally shipped 2026-09-03** as `app/crawl.py`, wired into DISCOVER at `discover.py:426` with six test files. §E's row already says so (re-read 2026-09-04); the residue is the Emits paperwork.
- **EUDAMED is reachable from the worker container, and the "container egress filter" reading was wrong.** Measured 2026-09-04: the same URL returns **HTTP 200 with real JSON when the request carries the pipeline's own `DentaliaComplianceBot/1.0` User-Agent**, and HTTP 502 with a "Web Filter" page when it carries httpx's default `python-httpx/...`. The filter is **User-Agent based, not path based**, and the pipeline never sends the UA that trips it (`app/adapters/fetcher.py:28,78`). Both the earlier 502-vs-200 readings were taken with a bare httpx or curl call, which is not the code path the pipeline uses. The account at `eudamed.py:165-183` — throttling that degrades to an empty page, seen from host and container alike — stands, and the `empty_sweep` guard is the right mitigation for it.
- **`CLAUDE.md:65` describes a deleted architecture.** It names a `scheduler` service under compose profile `full`. No such service exists; the crons run on the queue as eight self-deferring `scheduler.tick` jobs claimed by the worker.

| Registry | | Queue | |
|---|---|---|---|
| `document` | **870** — 203 production, 223 staged, 439 filed, 4 superseded, 1 rejected | done | 17.312 |
| `item_document` | **8.718** — 7.939 production, 414 staged, 365 retracted | dead | **79** — mostly upstream 404/timeout; 4 are a real `KeyError: 'archive_url'` |
| `evidence` | 6.867 | pending / running | **8 / 0** — all `scheduler.tick`, self-deferring, all armed |
| `manufacturer` | **384** (38 carry a playbook body, **6 carry a contact**) | extraction spend | **$41,32** over 1.719 calls (79% on T2) |

**Coverage: two true sentences, and the second is the one to quote.** **99,8% of the items BC flags as devices carry a production document. 28,8% of the catalogue does.** The gap is the 11.693 items whose class BC leaves blank, which the pipeline processes anyway (`process_md_unknown` defaults true) and which therefore belong in any honest denominator.

| Of the 15.958-item catalogue… | items | share |
|---|---:|---:|
| flagged a device by BC | 4.265 | 26,7% |
| **holds any production document** | **4.604** | **28,8%** |
| holds an article-level match | 1.902 | 11,9% |
| **holds an actual Declaration of Conformity** | **1.900** | **11,9%** |
| covered only by a manufacturer-scope claim | 2.702 | 16,9% |

Of the 4.604 covered items, 41,3% hold a DoC and 58,7% hold only a manufacturer-scope certificate.

**Document 816 is the single largest fact in the registry.** One EN ISO 13485 certificate, regulation `n.a.`, carries **2.567 CARL MARTIN items** — 60,2% of every BC-flagged device, and it is the *only* document any CARL MARTIN item links to (verified: one distinct `doc_id` across all 2.567 production links). C16 working as ruled. Remove it and MD coverage falls from 4.257 to **1.690 of 4.265 (39,6%)**.

**Concentration.** Only **11 of 367 manufacturers** carry any production-status document. Fourteen of the twenty largest by group count sit at exactly zero, including CEFLA (220 groups), IMES-ICORE (196), INTERDENT (167), BREDENT (150) and NSK (136). By BC code: 081 (2.567 MD items, 0 article-level), 001 (772/756), 008 (356/316), 077 (258/185), 005 (158/158), 275 (139/139), then 016/004/168 with twelve between them.

Production links by basis: `mfr-scope` 5.948 · `ref-list` 1.153 · `ref-item` 794 · `map-supplier` 43 · `manual` 1.

**What the automated discovery route actually yields — the finding that reframes the plan.** The 2026-09-03 random slice searched **310 groups**: search rung **131 hits against 162 misses**, 162 groups routed to a human dead end. It produced **83 documents** — **49 IFU, 26 DoC**, 4 EC, 2 ISO, 2 other — of which 30 carry no item link at all. The links it produced: **98 `fetch-context` staged** (capped by invariant 3), 8 retracted, and **2 `ref-list` production links.** The dominant product of open-web discovery is the *Instructions For Use*, which is a fact about what manufacturers publish openly, not about the code. **6.391 uncovered groups have never been searched**; at the slice's own rates, finishing the catalogue adds roughly 2.000 staged links, 1.650 staged documents and 3.300 manual tasks against about **41 production links**. Discovery is the route to a review queue, not to coverage.

**Where the registry came from** (joined on content hash, because `fetch_log.doc_id` is populated on only 12 of 1.060 rows): **755 documents from the hand-copied SFTP corpus (86,8%)**, 102 live web (11,7%), 16 email (1,8%). The corpus is 746 MB under `/imports`, gitignored; nothing in `app/`, `web/` or `tools/` reads the SFTP credentials in `.env`. **A fresh environment seeds empty and stays empty.**

**Review debt is growing about eighty times faster than it is emptied.** **223 staged documents** (oldest 2026-08-17), **414 staged links**, **364 open manual tasks** (173 `discovery-dead-end`, 166 `gate-manual`, 25 `dead-job-followup`) — **1.001 open human items**. There have been **13 `gate.apply` decisions in the project's history**, first 2026-08-13, last 2026-09-03: **0,6 a day**, against a queue that grew by roughly 250 yesterday alone. Reviewer work, not developer work (offer §7), and the only coverage available at zero extraction spend.

**Expiry, and who can actually be chased.** **86 production documents past effective expiry** (65 stated, 21 by the five-year staleness rule), touching **1.026 items**; **40 items have no unexpired evidence at all**. By owner: **IVOCLAR 59, VOCO 20, KOMET 3, HENRY SCHEIN 2, 3SHAPE 1, GC EUROPE 1**. Only **27 of the 86** sit behind a manufacturer we hold an address for — **IVOCLAR alone holds 59 and has no address anywhere in the corpus**, so the single highest-value contact ask is one company.

**The outbound chain produced its first addressed draft, 2026-09-04.** Nine drafts had sat since 20 August with empty `to_addrs` and nobody had checked whether the chain picks up the contacts landed on 09-03. One `email.request` enqueued through the real queue for expired VOCO doc 788 produced **draft 27, addressed `info@voco.de, dealersupport@voco.de`, covering all 20 expired VOCO documents, with a reminder armed.** S2.4's outbound half is functionally complete; what remains is a mailbox (G8), a human, and 378 manufacturers with no address.

**One cron has now produced work, and one alert path exists.** Eight crons tick correctly as self-deferring queue jobs. The untracked `.env` sets `SCHEDULER_COVERAGE_SCAN_ENABLED=true`, and on 2026-09-04 08:15 UTC the coverage-scan tick (job 35890) emitted **25 `discover.group` jobs**, all done — the `caused_by` query that returned 0 at the audit returns **25** (re-measured 2026-09-04 11:30). The other seven have still never emitted anything: `scheduler_run.job_id` is set only on `report.weekly`'s two rows. Every emission-gating flag is false in the live worker. **The only alert path landed 2026-09-04:** `app/alerts.py` POSTs one line to `alerts.webhook_url` (ntfy) when a job dead-letters, called from `runner.py` after the commit, and nothing else calls it. A dead worker, a stalled queue or a cron that stopped firing still reach nobody (`docs/dev/limits.md`): visible only as a red chip inside a password-protected page and a stale heartbeat row, both of which need a human to go and look. `service_heartbeat` also retains rows from dead containers, so "a heartbeat exists" is not the question "a worker is alive".

**INGEST has never run.** Zero `ingest.run` rows have ever existed in the job table and all 15.958 `item_mirror` rows carry one identical `updated_at` of 2026-08-10 13:14:08. `SCHEDULER_INGEST_WATCH_DIR` is empty; the BC OData adapter raises unless records are injected. The bring-up sequence was rehearsed 2026-09-03/04 on a throwaway database by `scripts/bringup-rehearsal.sh` (runbook § Bring up a fresh environment): migrate → vendor-master → manufacturers seed → playbooks sync → `ingest.run`, 15.958 items mirrored, no failed or dead jobs. The dev database itself still has no `ingest.run` row, and the rehearsal covers neither live fetching nor container start.

**Job counts are floors.** `job` spans 16.901–37.709 holding **17.399 rows, 3.412 ids missing** inside the range, and no prune tool exists in the tree — rows were deleted by hand. Roughly 10.000 grouped items still have no surviving `resolve.group` row; `item_group_member` is the only evidence, a real gap against invariant 10.

**Nothing backs up the registry or the archive.** Postgres is a host bind mount at `$PGDATA_HOST`; the archive is the named volume `dentalia_archive_data` (**676 MB, 1.382 files**). No backup service, script, crontab entry or systemd timer — all four re-checked 2026-09-04. Ruled a separate commercial order 2026-09-03, so this is a disclosed risk rather than a contract gap.

#### Stage matrix — re-read 2026-09-04

Twenty job types in the closed enum, **no emits drift against PRD v3 in either direction** (every stage's `queue.enqueue` calls checked against its Emits row). `playbook.reonboard` was the last unbuilt tag and **another session landed its probe branch 2026-09-04 08:20** — its failure-monitor branch still raises, and the emitting code at `scheduler.py:341-353` omits `index_url`, so enabling `SCHEDULER_FAILURE_REONBOARD_ENABLED` alone would dead-letter every job.

| Job type | Handler | done | dead | Last run | Real exercise |
|---|---|---:|---:|---|---|
| `resolve.group` | built | 9.946 | | 2026-08-27 | full catalogue; T1 adjudicator raises **by ruling** (band collapsed) |
| `validate.doc` | built | 2.953 | | 2026-09-03 | well exercised |
| `gate.candidate` | built | 2.798 | **42** | 2026-09-03 | well exercised |
| `extract.doc` | built | 991 | 14 | 2026-09-03 | T1/T2 genuinely live |
| `discover.group` | built | **355** | 1 | 2026-09-03 | **351 of 8.208 groups (4,3%)** |
| `fetch.url` | built | **211** | 21 | 2026-09-03 | 130 fetched, **51 refused as not-a-document**, 14 hash-dedupe, 14 recency-skip, 2 not-modified |
| `gate.apply` | built | 13 | | 2026-09-03 | **13 human decisions, ever** |
| `backfill.scan` | built | 11 | | 2026-08-21 | 9 of 15 corpus folders |
| `email.poll` | built | 7 | | 2026-08-21 | GreenMail fake only; IMAP host/user/password all empty |
| `email.request` / `.reminder` | built | 6 / 4 | | **2026-09-04** | **first addressed draft written 09-04** |
| `eudamed.sweep` | built | 6 | | 2026-09-02 | 4 manufacturers swept; mirror 16.827 |
| `report.weekly` | built | 4 | 1 | 2026-09-03 | the **only** cron that has ever emitted a job |
| `eudamed.sync` | built | 3 | | 2026-09-02 | button clicks |
| `eudamed.certregister` | built | 2 | | 2026-09-02 | manual only; cron off by default |
| `ingest.run` | built | **0** | | **never** | CSV/XLSX only; OData raises |
| `upload.ingest` | built | 0 | | never | never exercised live; no `upload` row in `fetch_log` |
| `vendor.import` | built | 0 | | never | never exercised live |
| `playbook.reonboard` | **part-built** | 0 | | never | probe branch landed 2026-09-04; failure-monitor branch raises |
| `scheduler.tick` | built | 0 | | **8 pending** | armed; **two jobs emitted, ever** |

**Acquisition, all of it.** `fetch_log` 1.060 rows: `backfill` 862, `live` 181, `email` 17, **`upload` never**. `discovery_log` 1.821 rows across **351 groups**: `search` 155 hit / 174 miss · `playbook` 75 hit / 329 miss · `manual` 174 hit · `recency` 9 hit · `known_url` 402 miss · `eudamed` 329 miss · `email` 174 skipped. The `vendor` rung has **zero rows of any outcome** — folded into `search` and unreachable through the live source-priority ladder.

**The EUDAMED rung can never hit, whatever the mirror's size.** It matches on Basic UDI-DI and **0 of 8.208 groups carry one**. 329 misses, zero hits, ever. `eudamed_mirror.cert_refs` being unpopulated on all 16.827 rows is a second independent reason; the missing group key is the binding one.

**The SRN bootstrap probe is unreachable through any UI or scheduler path — a clean deadlock.** `eudamed.py:749` runs `probe_srn` only when `trusted_manufacturer_srn` is empty; `web/registry.py:1626` refuses the release with HTTP 400 on the same condition; the scheduler only proposes manufacturers already in that table. The code comment at `eudamed.py:751-753` names GC EUROPE as the exact case the probe exists for, and **GC EUROPE carries 356 MD items, zero certificates, no SRN row and no `eudamed_sweep_state` row.**

**EUDAMED as mirrored.** `eudamed_certificate` **4.788** · `eudamed_mirror` **16.827** across **5 SRNs / 175 Basic UDI-DI** — IVOCLAR 11.366, CARL MARTIN 3.596, KOMET 1.810, MODJAW 8, plus **47 rows under `TW-MF-000012356`, an SRN with no row in `manufacturer_srn` at all**. `manufacturer_srn` **37** (28 auto, 8 pending, 1 confirmed) · `eudamed_sweep_state` **27**, 4 swept. The mirror reaches **4 of the 9 MD-carrying manufacturers**. All 12 EUDAMED/certificate views are read by real code; none is dead weight. Two of the four finding types — `certificate_status_alert` (1 row) and `certificate_gap` (44 rows) — **stop at UI display with no downstream action at all**.

**822 items sit permanently behind robots refusals, correctly honoured.** `refused_host` holds 5 rows: KAVO DENTAL (331 items), AMANN GIRRBACH (196), PRITIDENTA (193), COLTENE (102, Cloudflare bars rendering too). A client walkthrough should be steered away from them.

**58 of 203 production documents can never enter a supersession chain** — no `validity_from`, 53 of them DoCs. Null is never compared, so VALIDATE flags `downgrade-uncomparable` and caps at staged. Correct and safe, and it means more than a quarter of the production registry sits outside the mechanism that keeps it current. Same-day clusters are already waiting: IVOCLAR holds ten production declarations sharing 2025-11-24, VOCO nine sharing 2026-01-14.

**The audit trail records more repairs than gate decisions.** Of 2.156 `audit_log` entries: `tool:repair-document-manufacturer` 710, `gate` 673, `ops:repair-archive-urls` 389, `ops:repair-mfr-overbind` 356, `admin` 22, `tool:repair-mfr-task-reason` 6. **1.461 written by hand-run repair tools against 673 by the pipeline.** Every human decision is `admin`, because Caddy carries one shared credential — a config gap, not an architectural one, since `basic_auth` supports multiple named users.

**No test carries a document through more than one queue hop.** 2.623 tests across 122 files; every "end-to-end" test drives one handler through the runner and asserts on the *next job's payload* without executing it. The strongest multi-stage proof, `test_verification_hooks.py::test_qms_cert_binding_path`, walks four stages against real Postgres and a real PDF but hand-relays each payload. Five tests skip silently without the corpus, the vendor export or an API key. `POST /playbooks/{slug}` (`web/registry.py:2041`) has no HTTP-layer test.

**Playbook-steered extraction has barely run.** `extraction_attempt` carries a `playbook_rev` on **83 of 1.670** rows.

**Data quality.** `data_anomaly` 19.341 rows: `md_class_blank` **11.693** (73% of the catalogue), `mfr_ref_missing` 7.082, `mfr_ref_prose` 364, `cert_reference_unresolved` 123, `no_text_layer` 71, `t0_ref_template_miss` 4, `manufacturer_code_blank` 2, `basic_udi_check_failed` 2.

**Config flags off right now:** `DISCOVER_HOLD` on · **`SCHEDULER_COVERAGE_SCAN_ENABLED` on** (cap 25/day, set in the untracked `.env`, bypasses the hold by design) · `DISCOVER_EMAIL_RUNG_ENABLED`, `SCHEDULER_EXPIRY_EMAIL_ENABLED`, `SCHEDULER_EXPIRY_REDISCOVER_ENABLED`, `SCHEDULER_EMAIL_POLL_ENABLED`, `SCHEDULER_EUDAMED_CERTREGISTER_ENABLED`, `SCHEDULER_FAILURE_REONBOARD_ENABLED` off · `SCHEDULER_INGEST_WATCH_DIR` and all three `IMAP_*` credentials empty. All deliberate, all visible on `/scheduler`.

**Every remaining critical-path item, verified in the code on 2026-09-04.**

| Item | Verified how | State |
|---|---|---|
| Pass `visibility_timeout_s` into `queue.claim` | `runner.py:68` passes it, `queue.fail` gets both backoff keys, `run_once(queue_cfg=)` loads `cfg.queue`; `tests/test_runner.py` | **CLOSED** 2026-09-04 — this table was written after the closing commit and missed it |
| Registry + archive backup | no `backups/`, no `pg_dump` outside `schema_drift.py`, no crontab, no systemd timer | **OPEN** — go-live blocker, `[no-registry-or-archive-backup]`, separate order |
| Alert path off this machine | `app/alerts.py` + `runner.py` POST to `alerts.webhook_url` on dead-letter only (landed 2026-09-04); worker death, stalled queue, silent cron still unalerted (`docs/dev/limits.md`) | **PARTLY CLOSED** 2026-09-04 — the remaining half is still a go-live blocker |
| EUDAMED reachability | measured 09-04 from the worker with the pipeline's own UA: **HTTP 200, real JSON** | **NOT A BLOCKER** — the 502 only appears under httpx's default User-Agent, which nothing in `app/` sends |
| EUDAMED SRN probe deadlock | `probe_srn` runs only inside `handle_eudamed_sweep` (`eudamed.py:733-766`); the release route refuses with 400 on the same empty-SRN condition (`web/registry.py:1807-1813`) and is the only enqueue site (`:1816`) | **OPEN, new** — GC EUROPE (356 items) cannot be bootstrapped |
| `item_group.basic_udi_di` | 0 of 8.208 | **OPEN, new** — the real reason the eudamed rung is dark |
| Acceptance thresholds N and X (G15) | nothing in `app/config.py` or `docs/specs/kpi.md` sets either | **OPEN** — acceptance can be neither passed nor failed |
| Calibration map | `Config` has no `calibration` field at all; `gate.py` reads `getattr(cfg, "calibration", {})` and always gets `{}` — nothing to load from, not an empty load | **OPEN** — `[gate-calibration-map-absent]` |
| Expiry visibility | `expiring_documents` (`report.py:69`) filters `d.status='production'`, no `item_document_production` join | **OPEN** |
| Discovery state on the item page | `web/app.py` imports `discovery_state` from `app.coverage`, template receives `discovery`; `tests/test_coverage.py` | **CLOSED** 2026-09-04 |
| Multiple Caddy reviewers | one `basic_auth` pair; all 22 human audit rows are `admin` | **OPEN** — config only |
| EUDAMED `cert_refs` | no INSERT/UPDATE anywhere; 0 of 16.827 | **OPEN** |
| EUDAMED sweep per-page commit | no `conn.commit()` inside the page loop | **OPEN** |
| `MAX_VISION_PAGES` | still `4` (`app/extract/llm.py:44`) | **OPEN** |
| Manufacturer contacts | **6 of 384**; outbound chain proven live 09-04 | **PARTLY CLOSED** — IVOCLAR is the one ask worth 69% of the expired pile |
| `robots.txt` | `robots.check` is called by the crawl rung (`discover.py:491`) and the probe (`playbook_probe.py:136`) since 09-03; live FETCH of direct URLs still does not consult it and neither caller passes `fetch.robots_ttl_hours`; `refused_host` holds 5 real rows | **PARTLY CLOSED** — `[robots-reader-unwired]` narrowed |
| Device-type guard on GATE's REF branch | `_is_md_document` at `gate.py:1006` | **CLOSED** 2026-09-03 |
| Mis-typed ISO documents | 5 remain, all staged; doc 420 re-typed | **CLOSED at production** |
| Month-name / line-split dates | `_LONG_DATE` landed; line-split still returns None | line-split **OPEN** |
| Budget caps | display-only at `web/app.py:1710,1712` | **RELABEL ONLY** — ruled 2026-09-03 |
| Playbook crawl recipe / onboarding wizard | `app/crawl.py` live; probe and guided onboarding queue both landed 2026-09-04 (`web/onboarding.py`, migration 060), all at HEAD | **LANDED** — residue in `docs/dev/limits.md` § S2.2: no suggestion engine, no authenticated portals, `allow_hosts`/`pagination` not authorable in the browser, failure-monitor branch raises |
