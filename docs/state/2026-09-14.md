# State reading — 2026-09-14

Read straight off the dev Postgres at the 2026-09-14 tree, 08:22–10:50 UTC, by the fifth
readiness audit ("Dentalia Pipeline Readiness", fifth run,
2026-09-14, state"). The previous reading is [2026-09-11.md](2026-09-11.md); **36
commits and three days** lie between them. The stack had been up since 08:22 UTC;
running images were built 2026-09-11 20:15 UTC.

**A dated snapshot, not a live document.** Nothing here is maintained after the
date in its title; a newer reading gets a new file and `PHASES.md` repoints. Where
this disagrees with `PHASES.md`, `PHASES.md` wins.

Claims the previous reading made that this one overturns or narrows:

- ~~"1.806 items hold a Declaration of Conformity"~~ — **2.047**, and 2.005 hold an
  article-level match. The 09-11 revision already carried 2.047 after NEODENT's
  template; the morning figure is what the file recorded.
- ~~"the reminder ladder cannot climb"~~ — it climbs (2026-09-11) for request 21;
  requests 17–20 remain `awaiting` with their reminder jobs `done`, so nothing
  advances them. A residue, not the defect.
- ~~"robots.txt is not read on direct FETCH"~~ — it is (2026-09-11).
- **New, and fixed the same day:** `./scripts/deploy.sh --check` reported green
  while the running `web` image predated three `web/`-only commits, because
  `app/version.py` hashed `app/` and `migrations/` only and `GET /missing`
  answered 404. `web/` is a third hashed root now, the images are rebuilt and
  verified, and `/missing` is in the sidebar.
- **New, and fixed the same day:** `web/bc_push_view.py` joined
  `eudamed_certificate` on the bare certificate number while the push handler's
  join is SRN- and revision-scoped — 55 of 123 production documents with a
  certificate number match a foreign actor that way, 640 items touched. Both now
  read one shared query.
- **Wrong when first filed, corrected the same day:** "four corpus folders have
  no recorded reason". `docs/build-log.md` records the pre-flight and both
  refusals. Re-measured: DENSTPLY, DENTAURUM, PLANMECA and BREDENT hold 579
  catalogue items between them and **zero** `md_flag` items, so no scan of them
  can move AC1.
- **Ruled the same day (Denis):** AC1 target lowered 50% → **33%**, same
  definition; met at 34,2%.

| Registry | | Queue | |
|---|---|---|---|
| `document` | **883** — 203 production, 236 staged, 439 filed, 4 superseded, 1 rejected | ids | 16.901–38.365, **17.862 rows, 3.603 missing** |
| `item_document` | **8.981** — 8.180 production, 432 staged, 369 retracted | dead | 42 `gate.candidate`, 35 `fetch.url`, 14 `extract.doc`, 1 each `discover.group`, `playbook.reonboard`, `report.weekly` |
| `evidence` | 7.485 | pending | 10 `scheduler.tick` + 1 `email.reminder` (runs 2026-09-18) |
| database | **64 MB**; archive volume **744 MB** | extraction spend | **$42,02** over 1.753 calls (79% on T2) |

## Coverage

| Of the 15.958-item catalogue… | items | share |
|---|---|---|
| hold any production document | 4.845 | 30,4% |
| flagged a device by BC (`md_flag`); 11.693 blank, 0 false | 4.265 | 26,7% |
| of those, hold any production document | 4.257 | 99,8% of flagged |
| hold an actual Declaration of Conformity | 2.047 | 12,8% |
| hold an article-level match (`ref-list`, `ref-item`) | 2.005 | 12,6% |
| **AC1:** flagged devices with an article-level DoC (bases per the 2026-09-11 ruling) | **1.460** | **34,2% of flagged**; target 50% = 2.133 |
| of those, DoC unexpired | 488 | 11,4% of flagged |
| covered only by a manufacturer-scope certificate | 2.796 | 57,7% of covered |
| flagged devices covered only by a manufacturer-scope certificate | 2.702 | 63,5% of covered devices |
| hold a DoC whose document is `superseded` and no current one | 94 | — |

Queries: `item_document_production` joined to `item_mirror` and `document`;
AC1 restricts to `type='DoC'`, `md_flag`, and
`match_basis IN ('ref-list','ref-item','basic-udi-di','map-supplier')`. The
status board's headline ("Declarations on file for 1460 of 4265") agrees.

Concentration: document 816 (ISO, CARL MARTIN) 2.567 links; 255 and 44 (IVOCLAR
EC, ISO) 1.069 each; 138 and 289 (GC EUROPE) 356 each; 521 (NEODENT DoC) 279.
**12 of 384** manufacturers hold any production document.

## Discovery

| | |
|---|---|
| groups | **8.208**; 476 ever searched (5,8%); 7.732 never |
| searched groups where a rung found a candidate | 227 |
| searched groups whose only hit is the `manual` dead end | **249** (511 items, 178 devices; 2 of the groups hold any document) — the item page reads "Searched, found something" |
| groups with no production document | 6.711, of which 6.246 never searched |
| coverage cron | on, cap 25; fired 09-04, 07, 09, 11, 12, 14 (6 of 10 days); 150 `discover.group` since 09-04 |
| yield since 09-04 | 13 documents, all staged (2 DoC, 10 IFU, 1 ISO); 12 staged links; 0 production |
| `discovery_log` by source | known_url miss 552 · eudamed miss 477 · playbook hit 79 / miss 477 · search hit 212 / miss 265 · manual hit 265 · email skipped 265 · recency hit 9 |
| crawl rung | 64 crawl-shaped rows, all EDENTA, all hits; `crawl_link_rank` 1 row (2026-09-14 06:48). NSK and ULTRADENT groups last searched 2026-09-03 |
| `fetch_log` | backfill 862 · live 213 · email 17; `fetch.url` done via httpx 218, Playwright 11 |
| corpus | 9 of 15 device folders scanned (08-12 to 08-21). Never scanned: DENSTPLY 405 PDFs, DENTAURUM 37, PLANMECA 8, BREDENT 1; SOLVENTUM empty. Only NEODENT carries `skip_backfill` |

## Review, expiry, renewal

| | |
|---|---|
| open `manual_task` | **472** — 251 discovery-dead-end, 183 gate-manual, 38 dead-job-followup |
| staged | 236 documents, 432 links (327 items) |
| human decisions | 13 `gate.apply`, last 2026-09-03 13:32 UTC; every human audit row `decided_by='admin'` |
| expired production documents | **86** — 65 stated, 21 five-year staleness; IVOCLAR 59, VOCO 20, KOMET 3; 1.026 items touched; 40 items with only expired evidence (3 flagged devices) |
| no computable expiry | 25 production documents; 53 production GC DoCs with `validity_from` null |
| expiring within 180 days | 3 |
| renewal requests | 17–20 (IVOCLAR, KOMET, VOCO, GC EUROPE) `awaiting`, escalation 1, reminder jobs `done` since 08-21; 21 (VOCO) `requested`, reminder job 37719 due 09-18 |
| drafts | 10 `draft`, 1 `cancelled`; nothing sends |

## EUDAMED

| | |
|---|---|
| mirror | 16.827 devices under 5 SRNs (IVOCLAR 11.366, CARL MARTIN 3.596, KOMET 1.810, MODJAW 8, 47 unattributed); 4.788 certificates, 3.252 actors; last pull 2026-09-02 |
| SRNs | 27 manufacturers trusted (28 auto + 1 confirmed rows); 15 pending; 344 without a row; `srn_probed_at` null on all 384 |
| group keys | `item_group.basic_udi_di` set on 0 of 8.208; exact reference match under a trusted SRN reaches 3.288 items |
| views | certificate_gap 44 · certificate_drift 3 · drift candidates 4 · certificate_status_alert 1 · declaration_gap 74 · sweep_due 23 |

## Automation and the machine

| | |
|---|---|
| crons | 10 armed; coverage-scan (150 jobs) and report.weekly (2) emit; expiry-scan actions, failure-monitor, email.poll, certregister, ingest.monthly (watch dir empty), bc.push-drift all off; sweep-due and health-watch emit nothing by design |
| live flags | `DISCOVER_HOLD=true` · `SCHEDULER_COVERAGE_SCAN_ENABLED=true` cap 25 · IMAP host/user/password empty · no `BC_*` variable · `ALERTS_WEBHOOK_URL` set (ntfy) |
| alerting | `alert_state` 0 rows; one worker heartbeat; `/healthz` never opens the database; no dead-man's switch |
| deploy | `deploy.sh --check` exit 0: worker and web `app` digest = tree, schema = migrations. Web image built 2026-09-11 20:15 UTC; three later commits (2026-09-11 to 2026-09-14) change `web/` only; `web/missing.py` absent in the container; `/missing` → 404 |
| persistence | Postgres bind mount at `$PGDATA_HOST` (required by compose); archive volume 744 MB; no crontab, timer or script backs either up |
| machine | stack started 08:22 UTC; 0 restarts; 27 worktrees; office-UI t6 (one unmerged commit) and t7 (uncommitted) in flight |
| tests | 3.022 `def test_` in 138 files; 12 of 18 job families handler-only; no runner-level chain; suite not run for this reading |
| trackers | followups 217 open (207 tags), todo 0 open; PHASES § 2.2 lists three items closed 2026-09-11 |

## Rulings taken on this reading (Denis, 2026-09-14)

| AC / subject | Ruling | State after it |
|---|---|---|
| AC1 | Target lowered 50% → **33%**, same definition | **Met**, 1.460 of 4.265 = 34,2% |
| AC3 | **Met by construction, not by sampling** — the error rate was driven down by fixing its causes, each with a test. The 200-link audit is not to be built | Met; calibration loses its only planned source of labelled data |
| AC6 | **Re-based** on BC-flagged device groups holding a production document by any route, corpus and discovery alike | **Met**, 1.391 of 1.396 = 99,6%. The whole-catalogue reading (227 of 8.208) is retired |
| AC8 | **Per-item supersession.** A newer declaration replaces an older one only for the articles it names, only when genuinely newer, only within the same type and regulation. A supplier retiring an article is ordinary lifecycle, so requiring the newer document to name every article of the older one would treat the normal case as an anomaly | Still failing until built; the only AC that does |

**Acceptance after these four: eight of nine met, AC8 the single failure, and it is one defect.**

## The declaration gap is one supplier

| Manufacturer | Flagged device items with no article-level DoC |
|---|---|
| **CARL MARTIN** | **2.567** |
| IVOCLAR | 92 |
| KOMET | 91 |
| GC EUROPE N.V. | 40 |
| everything else | 15 |
| **total** | **2.805** |

Carl Martin is **91,5%** of the gap. They hold four documents: doc 816 (ISO) carries all 2.567 items by
manufacturer scope, and the two current MDR declarations (818, 819) link **zero** items. Two measured causes:
both are **scans with no text layer** (`document_text.source='none'`, 0 chars) and the vision tier returned a type
and a scope but no article list; and the older MDD declaration (817) states coverage as **ranges** —
`["1-9999","E1-E9999","M1-M9999","LS1-LS9999","CM1-CM9999"]` — while our article numbers are `13`, `1D`,
`280/1`, exactly what those ranges cover, and the REF gate compares exact strings.

886 Carl Martin groups exist; **5 have ever been searched**.

**Arithmetic:** solving this one supplier takes AC1 from 34,2% to ~94%. Without it the ceiling is 1.698 of
4.265 = **39,8%**, so no target above 40% is reachable by any other route.

**The ask is already written.** `email_draft` 26, kind `gap-request`, composed 2026-09-02: Dentalia's own approved
wording, then 70 Basic UDI-DI groups covering 2.389 Carl Martin articles, each with three of our own article
numbers underneath. `to_addrs` is `{}` — **no contact email exists for Carl Martin, or for any manufacturer in the
gap list** (6 of 384 carry a contact; none of the six is one we need).

## Findings that turned out not to be defects

- **F12** (grouping history) — **withdrawn.** `item_group_member.match_basis` records the decision for all 15.958
  items (8.208 singleton, 7.750 name-family). The pruned jobs held the score and the runners-up, not the decision.
  `[grouping-history-purged]` narrowed to its true size.
- **F21** (five robots-refused hosts) — **not a defect.** Obeying a refusal is the design; the 822 items reach
  their documents by email or by hand.
- **F41** (unscanned corpus folders) — **withdrawn**, see above.
